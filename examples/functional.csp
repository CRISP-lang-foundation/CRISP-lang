let @data1 = [3, 1, 4, 1, 5, 9, 2, 6];
let @data2 = [2, 7, 1, 8, 2, 8, 1, 8];

# Functional pipeline: zip → map → filter → take
let @numbers = data1
    .zip(data2)
    .map(|pair| => pair[0] * pair[1])
    .filter(|n| => n > 5)
    .take(4);

say @numbers;    # [6, 7, 8, 10]
